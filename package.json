{
  "devDependencies": {
    "@biomejs/biome": "^2.2.3",
    "@cspell/dict-en-au": "^1.1.7",
    "@vitest/coverage-v8": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "cspell": "^8.19.4",
    "happy-dom": "^18.0.1",
    "html-validate": "^10.0.0",
    "markdownlint-cli": "^0.45.0",
    "vitest": "^3.0.9"
  },
  "scripts": {
    "test": "vitest run --coverage",
    "coverage": "vitest run --coverage",
    "check": "biome check",
    "spell": "cspell '**/*.{js,css,html,md,json}'",
    "check_markdown": "markdownlint --ignore node_modules --fix ./**/*.md"
  }
}
