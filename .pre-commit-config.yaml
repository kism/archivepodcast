repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: ruff format
        description: Run ruff format on Python files using local installation
        entry: .venv/bin/ruff format
        language: system
        types: [python]
