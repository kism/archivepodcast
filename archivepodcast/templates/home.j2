<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>
        {{ settings['web_page']['tiltle']|e }}
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="X-Clacks-Overhead" content="GNU Terry Pratchett" />
    <link rel="stylesheet" href="/static/archivepodcast.css" />
    <script src="/static/clipboard.js"></script>
</head>

<body>
    <main>
        <h1>{{ settings['web_page']['tiltle']|e }}</h1>
        <p>{{ settings['web_page']['description']|e }}</p>
        <p>For instructions on how to add a podcast url to a podcast app, <a
                href="{{ settings['web_page']['podcast_guide'] }}">check out this helpful guide</a>.</p>
        {% for podcast in podcasts -%}
        <h2>{{ podcast['new_name']|e }}</h2>
        <p>{{ podcast['description']|e }}</p>
        <input type="text" readonly="readonly"
            value="{{settings['inet_path']}}rss/{{ podcast['name_one_word']}}"
            id="{{ podcast['name_one_word']}}">
        <button class="button" onclick="grabtoclipboard('{{ podcast['name_one_word']}}')"
            id="{{ podcast['name_one_word']}}_button">Copy url</button>
        {% endfor %}
        {% if about_page %}
        <footer>
            <hr>
            <a href="/about.html">About</a> | Contact: <a href="mailto:{{ settings['web_page']['contact']|e }}">{{
                settings['web_page']['contact']|e }}</a>
        </footer>
        {% endif %}
    </main>
</body>

</html>
